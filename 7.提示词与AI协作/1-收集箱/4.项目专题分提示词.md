## 分析项目技术栈

请分析本项目所采用的技术栈，包括前端、后端、数据库、部署环境及关键第三方服务，并说明各技术组件的选型理由、版本信息、在系统架构中的作用及相互集成方式。

## 将刚刚的环境安装和配置，以及服务启动整理成可复现的操作文档

请将前述环境安装、配置及服务启动的完整操作步骤，整理为一份结构清晰、步骤详尽、可完全复现的中文操作文档，要求包含：  
- 操作系统版本和硬件环境说明（如 Ubuntu 22.04 LTS，x86_64）；  
- 所需依赖软件及其精确版本（如 Python 3.11.9、Docker 26.1.4）；  
- 每一步命令（含必要参数和预期输出提示）；  
- 关键配置文件的修改位置与内容示例；  
- 服务启动、验证及常见问题排查方法；  
- 文档格式为纯文本或 Markdown，避免模糊描述（如“大概”“可能”“稍等”），所有操作须具备确定性与时序逻辑。

## 评估与优化

请基于以下草稿文件（路径：`@/Users/admin/Downloads/Code/openOii/doc/新手操作手册构建提示词.md`），对该新用户页面操作指引手册的初稿进行专业评估与优化：  
1. **评估重点**：检查内容完整性、逻辑清晰度、语言准确性、可复用性；  
2. **优化目标**：生成一份通用化、结构化、开箱即用的新手操作指引手册模板，适用于同类Web应用的新用户引导场景；  
3. **输出要求**：聚焦核心操作流程，补充必要的上下文说明与使用说明（如适用范围、更新维护建议）；  
4. **交付格式**：纯文本 Markdown，层级清晰，含标题、章节编号、简明步骤描述及必要注意事项。

## 新手操作手册v1

请基于我提供的代码、现有文档、前端页面和实际操作流程，输出一篇“新手操作手册”。

约束条件

要求：

- 这是操作手册，不是功能说明书
- 面向第一次接触项目的新手
- 重点写操作顺序、依赖关系、判断标准
- 必须优先依据源码和真实页面，不要猜
- 先告诉用户做什么，再简短解释为什么
- 每个主步骤统一使用：
  - X.0 界面结构
  - X.1 操作顺序
  - X.2 判断标准
  - X.3 关键说明
  - 完成标志
- `界面结构` 用表格
- `操作顺序` 用“步骤 1 / 步骤 2 / 步骤 3”子标题
- 页面复杂时，允许补少量服务操作的辅助表格
- 不要写成长篇纯文字
- 不要让辅助信息抢主流程篇幅
- 请主动识别哪些内容是主流程，哪些内容只是可选项或 Beta 能力
- 页面名称、按钮名称、状态名称尽量与真实界面一致
- 输出 Markdown 正文即可

### Markdown 输出规范
保证结构清晰、易于后续修改维护
内容结构必须严格包含以下模块（按顺序排列，标题层级统一）：
- 文档说明（含「文档职责」「适用场景」「阅读目标」三项带 > 引用格式的简明定义）

## 新手操作手册v2
• 请基于我提供的源码、现有文档、前端页面和实际操作流程，输出一篇《新手操作手册》。

  任务目标：
  - 面向第一次接触项目的新手
  - 重点还原首次上手的主流程，而不是编写功能说明书
  - 优先写清楚操作顺序、前后依赖、判断标准

  信息要求：
  - 以源码、真实页面、实际流程为准
  - 现有文档只能作为辅助参考
  - 无法从代码或页面确认的内容不要补写
  - 页面名称、按钮名称、状态名称尽量与真实界面一致

  写作原则：
  - 先告诉用户做什么，再简短解释为什么
  - 先梳理主流程，再组织文档，不要按菜单顺序罗列功能
  - 主动区分主流程、可选项、Beta/实验能力
  - 主流程进入正文；可选项和 Beta 能力只做简短补充
  - 不写大段纯文字，优先使用表格、短段落和分步骤说明

  输出格式：
  - 输出 Markdown 正文
  - 文档开头必须包含 `文档说明` 模块，并用 `>` 引用格式简要说明：
    - 文档职责
    - 适用场景
    - 阅读目标

  每个主步骤统一使用以下结构：
  - X.0 界面结构
  - X.1 操作顺序
  - X.2 判断标准
  - X.3 关键说明
  - 完成标志

  细化要求：
  - `界面结构` 用表格
  - `操作顺序` 使用“步骤 1 / 步骤 2 / 步骤 3”子标题
  - 页面复杂时，可补充少量服务操作辅助表格
  - 辅助信息不要抢主流程篇幅

  如果你要更适合实际投喂模型，我建议再加一句收口话放在最后：

  请优先保证“新手照着就能完成操作”，其次才是解释完整性。

  ## 新手操作手册v3

  • 请基于我提供的源码、现有文档、前端页面和实际操作流程，输出一篇《新手操作手册》。

  目标：
  - 面向第一次接触项目的新手
  - 还原首次上手主流程，而不是编写功能说明书
  - 重点写清操作顺序、依赖关系、判断标准
  - 以“新手照着就能完成操作”为第一优先级

  信息约束：
  - 以源码、真实页面、实际流程为准
  - 现有文档仅作辅助参考
  - 无法确认的内容不要补写
  - 页面、按钮、状态名称尽量与真实界面一致

  写作要求：
  - 先梳理主流程，再写文档，不要按菜单顺序罗列功能
  - 先写做什么，再用短句解释为什么
  - 主动区分主流程、可选项、Beta/实验能力
  - 主流程写进正文；可选项和 Beta 能力简短补充即可
  - 不写大段纯文字，优先使用表格、短段落、分步骤说明

  输出要求：
  - 输出 Markdown 正文
  - 开头必须包含 `文档说明` 模块，并用 `>` 引用格式简要说明：
    - 文档职责
    - 适用场景
    - 阅读目标

  结构要求：
  - 文档正文以主流程为主线
  - 为保证文档清晰、便于维护，主流程步骤应尽量写清以下信息，但不要机械套用固定模板：
    - 当前入口或界面位置
    - 操作顺序
    - 成功后的判断标准或完成标志
    - 必要的依赖、限制或风险提示
  - 可根据项目类型灵活组织；如果某一步不涉及复杂界面或无需额外说明，可以省略次要部分，但必须保证新手能顺利完成操作

  细节要求：
  - 对界面型项目，优先补充“界面结构”，界面结构优先用表格
  - 对命令型或配置型项目，优先补充“执行入口、参数、结果判断”
  - `操作顺序` 优先用“步骤 1 / 步骤 2 / 步骤 3”
  - 页面复杂时可补少量辅助表格
  - `判断标准` 和 `完成标志` 尽量明确
  - 辅助信息不要盖过主流程